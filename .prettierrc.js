export default {
  // 每行代码的最大长度
  printWidth: 120,
  // 缩进的空格数
  tabWidth: 2,
  // 使用空格而不是制表符
  useTabs: false,
  // 句尾添加分号
  semi: true,
  // 使用双引号而不是单引号
  singleQuote: false,
  // 对象属性键使用引号仅在必要时
  quoteProps: "as-needed",
  // JSX中使用双引号而不是单引号
  jsxSingleQuote: false,
  // 多行时尽可能打印尾随逗号
  trailingComma: "all",
  // 在对象字面量的大括号之间添加空格
  bracketSpacing: true,
  // 将多行JSX元素的 > 放在最后一行的末尾，而不是单独放在下一行
  jsxBracketSameLine: true,
  // 箭头函数参数周围始终使用括号
  arrowParens: "always",
  // 不需要在文件顶部添加 @format 标记来格式化文件
  requirePragma: false,
  // 不需要在文件顶部添加 @format 标记来格式化文件
  insertPragma: false,
  // 保留HTML中的换行和空格
  proseWrap: "preserve",
  // 忽略HTML中的空白字符敏感性
  htmlWhitespaceSensitivity: "ignore",
  // 不要缩进Vue文件中的 <script> 和 <style> 标签
  vueIndentScriptAndStyle: false,
  // 文件末尾使用LF换行符
  endOfLine: "lf",
  // 自动格式化嵌入的语言（如CSS、JSX等）
  embeddedLanguageFormatting: "auto",
};