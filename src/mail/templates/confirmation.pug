doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title 确认您的邮箱
    style.
      body {
        margin: 0;
        box-sizing: border-box;
      }
      .layout {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 500px;
        margin: 0;
        background-color: #fffbeb;
      }
      .container {
        width: fit-content;
        padding: 2rem;
        background-color: #ffffff;
        border-radius: 14px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      }
      h1 {
        color: #d97706;
        margin: 10px 0 30px;
      }
      .button {
        display: flex;
        justify-content: center;
        align-items: center;
        background: #d97706;
        color: #ffffff;
        border-radius: 10px;
        font-size: 1rem;
        width: 120px;
        height: 40px;
        text-decoration: none;
        margin-top: 40px;
      }
  body
    .layout
      .container
        h1 Verify Your Email Address
        p You created an account with <strong>#{email}</strong>
        p Click “<strong>Confirm</strong>” to verify your email and unlock the full account features
        a.button(href=url) Confirm